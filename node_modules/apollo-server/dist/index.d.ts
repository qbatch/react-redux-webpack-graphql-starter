import * as express from 'express';
import { ApolloServerBase, ListenOptions, Config, ServerInfo } from 'apollo-server-core';
export * from './exports';
export declare class ApolloServer extends ApolloServerBase<express.Request> {
    private disableHealthCheck;
    private onHealthCheck;
    constructor({disableHealthCheck, onHealthCheck, ...opts}: Config<express.Request> & {
        onHealthCheck: (req: express.Request) => Promise<any>;
        disableHealthCheck: boolean;
    });
    listen(opts?: ListenOptions): Promise<ServerInfo>;
}
